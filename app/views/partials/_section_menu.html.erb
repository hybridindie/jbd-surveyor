<div class="surveyor_menu">
  <%= t('surveyor.sections') %>
  <ul>
    <% @sections.each do |s| %>
      <li class="<%= "active" if s == @section %>"><%= menu_button_for %></li>
    <% end %>
  </ul>
</div>
